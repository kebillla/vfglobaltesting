/* ==========================================================================
   VolFix Landing — конфигурация.
   Всё, что меняется без правки вёрстки: цена, ссылки, ID пикселей, тумблеры.
   ========================================================================== */
window.SITE_CONFIG = {

  /* --- Цена. Меняется здесь, подставляется во все блоки автоматически. ---- */
  price: {
    amount: 10000,          // число без пробелов
    currency: '₸',
    period: 'month',        // ключ периода: month | quarter | year (см. i18n)
    oldAmount: null         // например 15000 — покажет зачёркнутую старую цену
  },

  /* --- Бонус VolFix: длительность в неделях (подставляется во все тексты).
     Если менять число — проверить форму слова в i18n.js ({weeks} недели/апта). */
  volfixBonusWeeks: 2,

  /* --- Оплата: пока заглушка. -------------------------------------------
     provider: 'stub' — показывает экран-заглушку и ведёт на success.html
     provider: 'robokassa' | 'prodamus' — подставить payUrl, когда будет юрлицо */
  payment: {
    provider: 'stub',
    payUrl: ''              // URL платёжной формы, когда подключат эквайринг
  },

  /* --- Лиды: куда отправлять форму. --------------------------------------
     Пустая строка = ничего не шлём наружу, лид падает в localStorage + консоль.
     Подставить endpoint (Google Apps Script / CRM / /api/lead) при запуске.  */
  leads: {
    endpoint: '',
    storeLocally: true
  },

  /* --- Мини-квиз перед формой (сегментация лидов). true/false ------------- */
  quizEnabled: true,

  /* --- Честный urgency: реальное ограничение мест в потоке. --------------
     Поставить null, чтобы блок не показывался вовсе.                        */
  seats: {
    total: 50,
    left: 12                // обновлять вручную/из CRM. null = скрыть блок
  },

  /* --- Контакты и ссылки -------------------------------------------------- */
  links: {
    telegram: 'https://t.me/',       // публичный канал/бот для связи
    support: 'https://t.me/',        // менеджер поддержки
    volfix: 'https://volfix.net/',
    privacy: '#',
    offer: '#'
  },

  /* --- Аналитика. Пустая строка = скрипт не грузится вообще. -------------- */
  analytics: {
    gtmId: '',              // 'GTM-XXXXXXX'
    ga4Id: '',              // 'G-XXXXXXXXXX' (если без GTM)
    metaPixelId: '',        // '1234567890'
    tiktokPixelId: ''       // 'CXXXXXXXXXXXXXXXXXXX'
  },

  /* --- Язык -------------------------------------------------------------- */
  i18n: {
    default: 'ru',
    available: ['ru', 'kk']
  }
};
